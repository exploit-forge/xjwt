version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
    volumes:
      - ./frontend:/app
    ports:
      - "3000:3000"
    networks:
      - appnet

  backend:
    build:
      context: ./backend
    volumes:
      - ./backend:/app
    environment:
      - PORT=8000
    ports:
      - "8000:8000"
    networks:
      - appnet

  jwttool-worker:
    build:
      context: ./worker
    volumes:
      - ./worker:/app
    networks:
      - appnet

networks:
  appnet:
    driver: bridge
